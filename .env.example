# Copy this file to `.env` and update values. Do NOT commit your real `.env` with secrets.

# =============================================================================
# Docker Compose MySQL Environment Variables (for local development)
# =============================================================================
# MySQL root password (used only for local development)
MYSQL_ROOT_PASSWORD=Shriyansh@123

# Name of the database to create on first run
MYSQL_DATABASE=student_management

# Optional: set a non-root user and password if you prefer
# MYSQL_USER=sms_user
# MYSQL_PASSWORD=sms_password


# =============================================================================
# Backend Java Application Environment Variables (for production deployment)
# =============================================================================
# Database connection URL - Change this based on your deployment
# For Railway MySQL: mysql://root:password@containers-us-west-xxx.railway.app:port/railway
# For Render MySQL: mysql://username:password@hostname:port/database_name
# For local Docker: mysql://root:Shriyansh@123@localhost:3306/student_management
DB_URL=jdbc:mysql://localhost:3306/student_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Database username
DB_USER=root

# Database password
DB_PASS=Shriyansh@123
